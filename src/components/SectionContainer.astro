---
const { class: className, id } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`section ${className} w-full mx-auto mt-52 flex justify-center items-center flex-col  lg:w-[740px] pb-24`}
>
  <slot />
</section>